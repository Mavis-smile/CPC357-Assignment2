{
  "name": "hardware-bridge",
  "version": "1.0.0",
  "description": "Hardware bridge for Smart Bin - MongoDB + HTTP to GCP",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js",
    "dev": "nodemon bridge.js"
  },
  "dependencies": {
    "mongodb": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
